---
title: 可视化讲解索引
description: 按概念、相关学习单元（Learning Unit）和版本边界查找当前完整发布的可视化讲解。
pairId: visuals-index
counterpart: /en/visuals/
factCheckDate: '2026-08-30'
license: CC-BY-4.0
provenance: original
structure:
  - scope
  - published-index
  - sequence
  - evidence-boundary
head:
  - tag: meta
    attrs: { name: 'cuda:pair-id', content: visuals-index }
  - tag: meta
    attrs: { name: 'cuda:fact-check-date', content: '2026-08-30' }
  - tag: meta
    attrs: { name: 'cuda:license', content: CC-BY-4.0 }
  - tag: meta
    attrs: { name: 'cuda:structure', content: 'scope,published-index,sequence,evidence-boundary' }
---

import ResourceIndex from '../../../components/ResourceIndex.astro';

<a class="locale-pair" data-locale-counterpart href="/en/visuals/" lang="en">Read the English counterpart</a>

本索引只列出已经形成完整双语发布对（Publication Pair）、同时提供交互与静态回退的可视化讲解（Visual Explainer）。十四项正式讲解中，VIS01、VIS02、VIS03 warp divergence、VIS04 内存请求 segment 分组、VIS05 shared-memory bank 映射、VIS06 memory hierarchy/ownership/lifetime、VIS07 stream/event dependency、VIS08 managed-memory page migration、[VIS09 NVCC 构建产物流水线](/visuals/artifact-pipeline/)与 [VIS10 Reduction tree 与非活动通道](/visuals/reduction-stages/)使用十个独立页面；VIS19-VIS22 嵌入各自学习单元（Learning Unit）。VIS08 的先修条件是 M01、M02 与 M10，相关资源是 M10 与 EX08；VIS09 的严格先修条件是 M15、M16 与 M17，相关资源是 EX10；VIS10 的严格先修条件是 A02，相关资源是 EX11 与 Q02。四张内嵌讲解卡片直接深链到页内锚点，导航不会为它们复制四个独立页面。未完成的规划条目不会显示。

## 已发布可视化讲解

<ResourceIndex group="visuals" locale="zh-CN" />

## 顺序与直接查找

规划 ID 是稳定的讨论标签，不会生成或承诺固定 URL。卡片目的地可以是独立页面，也可以是学习单元内的明确锚点；你可以直接打开当前卡片，或先沿相关学习单元回到建立该概念的课程位置。

## 证据边界

VIS01-VIS10 与 VIS19-VIS22 都只在浏览器中运行确定性教学模型，提供静态或文字回退，并且不执行 CUDA。VIS08 的 residency rail 和 page transition 是 browser-only model，不是 fault、migration、residency、byte count 或 timing observation；VIS09 的 host/device branches、target plans 与 artifact flow 也是原创 browser-only model，不是 compiler execution、artifact inspection、runtime image selection 或 timing observation；VIS10 的八通道 reduction-stage ledger、inactive-lane states 和静态 stage diagram 回退同样是确定性教学模型，不是 CUDA execution、runtime trace、timing 或 performance observation。索引明确显示这一边界；十四项可视化讲解都没有 CUDA Evidence Status，也不会声明 Compile-Checked、Community-Observed 或 Runtime-Verified。复核日期：**2026-08-30**。
