<!-- SPDX-License-Identifier: Apache-2.0 -->
# EX18 Evidence Boundary

No CUDA compilation, GPU runtime, or performance record is retained here.
The manifest's compilation array and recorded observations remain empty;
runtime remains Pending Hardware Verification.

Host tests and build dry runs are not Compile-Checked evidence. Future compile
records must retain the exact profile, source identity, build-contract hash,
container/toolchain identity, all stage exit statuses, artifacts and linkage
reports. Runtime records additionally need a complete Environment Manifest,
loaded cuBLAS triplet/raw version, resolved library/package identity and hashes
for the unexposed build component, stdout/stderr, and all correctness verdicts.
